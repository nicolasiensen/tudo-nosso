.container.mt2
  .clearfix
    .col-8.mx-auto
      #document-component
      javascript:
        $(document).ready(function() {
          documentFlux.init({
            document: #{raw @document.to_json},
            currentUser: #{raw current_user.to_json}
          });
        });
